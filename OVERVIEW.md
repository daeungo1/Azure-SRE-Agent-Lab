# Azure SRE Agent 개요

> **5분 브리핑용 문서입니다.** 각 항목의 상세 설명·화면·설정 방법은
> **[SRE Agent 기능 설명](SRE_Agent.md)** 에 있습니다.
>
> 포털 [sre.azure.com](https://sre.azure.com) · 공식 문서 [learn.microsoft.com/azure/sre-agent](https://learn.microsoft.com/azure/sre-agent/overview)

---

## 1. 한 줄 요약

**새벽 2시 47분, 당직 폰이 울립니다. "결제 API 5xx 급증."**

- **원인 파악에만 1시간.** 관제 통보 → 담당자 호출 → VPN 접속 → 시스템별 로그인.
  조사를 시작하기도 전에 한 시간이 지나갑니다.
- **아는 사람이 없으면 원점.** 지난 분기 같은 장애의 원인과 조치는 개인 메모에만 남아 있습니다.
- **복구가 끝나도 반나절.** 타임라인 재구성, 장애 보고서, 재발 방지 대책, 경영층 보고.

<p align="center">
  <img src="docs/sre-agent-hook-timeline-ko.svg" alt="같은 장애에 대한 사람 대응과 Azure SRE Agent 자율 대응 타임라인 비교" width="100%">
</p>

> **Azure SRE Agent 는 이 세 구간을 대신합니다.**
> 당직자가 접속을 마쳤을 때, **원인 · 영향 범위 · 다음 조치가 채워진 조사 결과(Investigation)** 가 이미 기다립니다.
> 조사 내용은 팀 메모리에 남아 다음 장애 때 재사용됩니다.

| 구분 | 사람이 하던 방식 | SRE Agent 도입 후 |
|---|---|---|
| 알림 확인 | 사람이 깰 때까지 대기 | 즉시 Acknowledge |
| 도구 전환 | 탭 5개 이상 | 0개 (에이전트가 처리) |
| 조사 | 도구별 수동 상관분석 | 모든 소스 자동 질의 |
| 지식 축적 | 담당자 머릿속 | Memory 에 영구 저장 |
| 사후 보고 | 타임라인 수기 재구성 | 인시던트 리포트 자동 생성 |
| 공유 | 화면 캡처 · 경로 설명 | 스레드 링크 복사 → Teams/Slack |

---

## 2. 무엇을 하는 제품인가

Azure SRE Agent 는 운영 업무(toil)를 자동화하는 **Azure 관리형 AI 에이전트**입니다.
관측 도구 · 인시던트 플랫폼 · 소스 코드 저장소를 하나의 워크플로로 연결합니다.

경고가 발생하면 다음 다섯 단계가 **사람 개입 없이** 진행됩니다.

| 단계 | 무슨 일이 일어나나 |
|---|---|
| ① 신호 수신 | Azure Monitor · PagerDuty · ServiceNow 알림 수신 |
| ② 라우팅 | 심각도 · 서비스 · 키워드로 **담당 에이전트와 자율성 수준** 결정 |
| ③ 조사 | 관측 데이터 · 런북 · 코드/배포 이력을 **병렬 조회**하며 증거 수집 |
| ④ 원인 분석 | 근본 원인 가설 수립, 완화 방안 제시 |
| ⑤ 조치 & 기록 | 완화 실행(또는 승인 요청) · 경고 종료 · 리포트 생성 · 메모리 저장 |

→ 상세: [동작 흐름](SRE_Agent.md#1-동작-흐름)

---

## 3. 무엇으로 구성되는가

도입 후 조직이 실제로 다루게 되는 구성 요소는 여섯 가지입니다.

| 구성 요소 | 한 줄 설명 | 왜 필요한가 |
|---|---|---|
| **Response Plan** | 어떤 인시던트를 누가 어느 수준까지 처리할지 정하는 규칙 | 자율 수준을 인시던트별로 통제 |
| **Custom Agent** | 도메인별 전문 에이전트 (DB · 배포 · 비용 …) | 역할별로 권한과 도구를 분리 |
| **Knowledge Base** | 런북 · 아키텍처 문서 · 포스트모템 | 우리 조직의 판단 기준으로 결론 |
| **Skills / Hooks** | 절차서와, 실행 시점에 끼어드는 통제 규칙 | 권한을 줄이지 않고 행동을 제약 |
| **Automation** | 예약 작업 (헬스 체크 · 비용 점검 …) | 장애가 없는 날에도 동작 |
| **Team Memory** | 조사 결과가 누적되는 조직 지식 | 담당자가 바뀌어도 남는 자산 |

> **핵심 논점** — 에이전트는 *범용 챗봇*이 아니라 **조직의 절차와 통제를 담는 그릇**입니다.
> 위 여섯 가지가 곧 "우리 회사 운영 방식"을 에이전트에 이식하는 수단입니다.

→ 상세: [핵심 기능](SRE_Agent.md#4-핵심-기능) · 실제 구성 예시: [Portal 기능](features-sre/README.md)

---

## 4. 통제는 어떻게 하나

가장 많이 나오는 질문입니다 — *"AI 가 운영 환경을 직접 바꾸게 둘 수 있나?"*
SRE Agent 는 **서로 독립적인 세 계층**으로 통제합니다.

| 계층 | 무엇을 통제하나 |
|---|---|
| ① **관리 ID 의 Azure RBAC** | 에이전트가 Azure 리소스에 **할 수 있는 일** |
| ② **실행 모드** | 조치 전 **승인이 필요한지** (Review / Autonomous) |
| ③ **사용자 역할** | 사람이 포털에서 **할 수 있는 일** |

권한 수준은 생성 시 두 가지 중 선택합니다.

- **Reader (기본값)** — 조사는 전부 가능, 변경은 불가. 조치가 필요하면 **사용자 권한을 빌려(OBO)** 승인을 요청합니다.
- **Privileged** — 조사 + 제한된 범위의 직접 조치.

> **단, 표시된 권한 수준보다 실제 역할 할당이 우선합니다.** Azure 는 관리 ID 에 부여된
> RBAC 으로 허용 여부를 판정합니다. 이 Lab 은 권한 수준을 **Reader 로 둔 채**
> `Container Apps Contributor` 를 직접 부여해 **무인 완화까지** 수행하도록 구성했습니다
> → [Lab 의 권한 구조](README.md#권한-구조--조사와-조치의-경계)

> **쓰기 역할 없이도 조사·진단·리포트는 그대로 동작합니다.** 막히는 것은 재시작 · 스케일 · 설정 변경뿐입니다.
> 즉 "권한을 줄 수 없어서 도입을 못 한다"는 상황은 발생하지 않습니다.

→ 상세: [권한 모델](SRE_Agent.md#5-권한-모델-permission-model)

---

## 5. 어디에 적용되나

| 범주 | 대상 |
|---|---|
| Azure 리소스 | VM · App Service · Container Apps · AKS · Functions · Storage · SQL · Cosmos DB · PostgreSQL · Redis · 네트워킹 |
| 모니터링 | Azure Monitor · Application Insights · Log Analytics · Grafana |
| 인시던트 | Azure Monitor Alerts · PagerDuty · ServiceNow |
| 소스 · CI/CD | GitHub · Azure DevOps |
| 알림 | Teams · Slack · Outlook |

> 커넥터 없이도 관리 ID + RBAC 만으로 Application Insights · Log Analytics · Azure Monitor ·
> Resource Graph · Azure CLI · AKS 진단이 **즉시 동작**합니다.
> 커넥터는 **Azure 외부** 시스템(GitHub, PagerDuty 등)을 붙일 때 필요합니다.

---

## 6. 도입 전 확인할 것

- 채팅 인터페이스는 **영어만 지원**합니다. (런북·문서는 한국어로 색인되지만 프롬프트는 영어 권장)
- **리전이 제한적**입니다. 이 Lab 은 `eastus2` · `swedencentral` · `australiaeast` 를 사용합니다.
- 브라우저에서 `*.azuresre.ai` 도메인 접근이 필요합니다. (일부 프록시 · Zscaler 가 차단)
- **사용량 기반 과금(AAU)** 이며, 사용량은 Operations Hub 에서 확인합니다.
- 다른 AI 시스템과 마찬가지로 **잘못된 결론을 낼 수 있습니다.** 승인 전 검토가 전제입니다.
- 현재 **Preview** 입니다.

---

## 7. 실제로 되는가 — 검증 결과

이 저장소는 의도적으로 장애(OOM)를 주입하고 에이전트가 스스로 처리하도록 두는 Lab 입니다.
전 과정이 기록되어 있습니다.

| 항목 | 결과 |
|---|---|
| 탐지 → 완화 | **사람 개입 0회**로 완료 |
| 조치 내용 | 메모리 부족 컨테이너를 근거와 함께 스케일 조정 |
| 산출물 | 인시던트 리포트 + 팀 메모리 파일 3개 **자동 생성** |

→ [E2E 실행 결과](README.md#6-e2e-실행-결과) · 직접 재현: [Lab 실습](README.md)

---

## 다음 단계

| 목적 | 문서 |
|---|---|
| 기능을 자세히 보고 싶다 | [SRE Agent 기능 설명](SRE_Agent.md) |
| 직접 배포해 보고 싶다 | [Lab 실습](README.md) |
| 우리 조직 방식으로 구성하고 싶다 | [Portal 기능](features-sre/README.md) |
